import React from 'react';
import styled from 'styled-components';

const TodoContainer = styled.section`
  margin: auto;
`;

const Todo: React.FC = () => {
  return <TodoContainer>nigger</TodoContainer>;
};

export default Todo;
